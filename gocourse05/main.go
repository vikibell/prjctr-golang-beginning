package main

const serverUrl = "http://remote.animals.control"

func main() {
	//dayCamera := NewDayCamera()
	//nightCamera := NewNightCamera()
	//infraredCamera := NewInfraredCamera()
	//
	//memory := make(Memory)
	//processCameraData(dayCamera)
	//saveCameraData(dayCamera, memory)
	//sendCameraData(dayCamera, memory, serverUrl)
	//
	//processCameraData(nightCamera)
	//saveCameraData(nightCamera, memory)
	//sendCameraData(nightCamera, memory, serverUrl)
	//
	//processCameraData(infraredCamera)
	//saveCameraData(infraredCamera, memory)
	//sendCameraData(infraredCamera, memory, serverUrl)
}
